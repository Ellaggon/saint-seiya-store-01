---
import EditCategoryPage from "@/ui/pages/admin/categories/[id]/edit.astro";

const { id } = Astro.params;
const user = Astro.locals.user;
if (!user || user.role !== "ADMIN") {
  return Astro.redirect("/login");
}
---

<EditCategoryPage id={id!} />
